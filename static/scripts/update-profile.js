const updateBtn = document.querySelector('.updateBtn');
const updateText = document.querySelector('.updateProfile');

const showText = updateBtn.addEventListener('click', () => {
  updateText.style.display = 'block';
});
