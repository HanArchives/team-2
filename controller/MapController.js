// // Hier komt de back-end code voor de map --> Jonna//
// const map = async (req, res) => {
//   // const query = { park: req.query.park };
//   // const hondenmaatjes = await db.collection('matches').find(query).toArray();
//   // res.render('pages/match', { matches: hondenmaatjes });

//   console.log('kaas is kaas');

//   const query = { park: req.body.park };
//   // const query = { park: { $in: arrayify(req.body.park) } };
//   const dogs = await db.collection('matches').find(query).toArray();

//   res.render('pages/match', { matches: dogs });
// };

// module.exports = {
//   map: map,
// };
